import { RemoveInvalidCharsPipe } from './remove-invalid-chars.pipe';

describe('RemoveInvalidCharsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveInvalidCharsPipe();
    expect(pipe).toBeTruthy();
  });
});
